import { commentApprovalStatus } from 'ember-quickstart/helpers/comment-approval-status';
import { module, test } from 'qunit';

module('Unit | Helper | comment approval status');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = commentApprovalStatus([42]);
  assert.ok(result);
});
